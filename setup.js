document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("[data-check]").forEach(box=>{const key=`nexus-setup-${box.dataset.check}`;box.checked=localStorage.getItem(key)==="1";box.addEventListener("change",()=>localStorage.setItem(key,box.checked?"1":"0"))});document.querySelectorAll("[data-copy-code]").forEach(button=>button.addEventListener("click",async()=>{const code=button.closest(".code-block").querySelector("code").textContent.trim();try{await navigator.clipboard.writeText(code);button.textContent="kopiert";setTimeout(()=>button.textContent="kopieren",1400)}catch{button.textContent="markieren"}}))});